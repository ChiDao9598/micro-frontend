export const MAX_PLAYERS = 50;
export const OFFICE_HALF_SIZE = 18;

// Spawn positions — new players are placed slightly randomly near the center
// so they don't all stack on top of each other
export const SPAWN_RADIUS = 3;